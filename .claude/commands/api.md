Create a new Next.js API route for this project.

Read the existing API routes (packages/web/src/app/api/) to understand the patterns, auth checks, error handling, and response formats used.

Build a complete API route for: $ARGUMENTS

Requirements:
- Place in the correct location under app/api/ as a route.ts file
- Use Next.js 14 App Router route handler conventions (GET, POST, etc. named exports)
- Add authentication check using Clerk's auth() from @clerk/nextjs/server where appropriate
- Use proper TypeScript types for request body and response
- Return consistent JSON responses with appropriate HTTP status codes
- Add error handling with try/catch
- Validate input before processing
- Follow the same patterns as existing API routes in the project

Show the full endpoint URL and example fetch() call when done.
